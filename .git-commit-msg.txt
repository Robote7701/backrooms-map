feat: identite visuelle et immersion (OG image, ambiance, son, onboarding, export, exploration, clavier)

Partage social :
- public/favicon.svg + favicon.png + apple-touch-icon.png (icone d'onglet,
  raccourci mobile)
- public/og-image.svg + og-image.png (1200x630) : apercu genere pour
  Reddit/Discord/Twitter quand le lien est partage
- index.html : balises Open Graph + Twitter Card + theme-color

Immersion :
- CSS pur (grain, vignette, brouillard qui derive lentement) sur la carte,
  au-dessus du canevas Cytoscape, purement decoratif (pointer-events:none)
- audio/ambientHum.js + AmbientSoundToggle.jsx : bourdonnement de neon
  genere via Web Audio (2 oscillateurs desaccordes + bruit filtre),
  opt-in uniquement, aucun fichier audio a heberger

Confort :
- OnboardingHint.jsx : bulle d'aide a la toute premiere visite
  (localStorage), disparait a la premiere selection
- data/useVisited.js + StatsPanel : suivi d'exploration "X/103 niveaux
  explores" avec barre de progression
- data/exportLevelCard.js : carte-image (Canvas) d'un niveau
  telechargeable, pensee pour le partage social
- Navigation clavier : fleches gauche/droite sautent vers la premiere/
  derniere connexion du niveau selectionne, Echap ferme le panneau

Corrige au passage : l'emoji 📸 (export) ne s'affichait pas sous Windows,
remplace par une icone SVG inline (meme cause que le fix des drapeaux).

Traductions FR/EN/DE completes pour toutes les nouvelles chaines.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
